query {
  currentUser {
    email
    _id
  }
}

query ($userId: ObjectId!) {
  user(userId: $userId) {
    email
    _id
  }
}

query {
  streams {
    _id
    title
    description
    url
    author {
      email
      _id
    }
  }
}
